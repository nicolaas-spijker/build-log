# Workflow: publish a post

<!--
An example workflow. A workflow is the repeatable steps for one task,
written down once so it runs the same way every time. Replace this with a
real one of your own, and add a file per workflow.
-->

Steps:

1. [First step. Specific enough that the agent does not improvise.]
2. [Next step.]
3. [Next step.]

Done when: [the concrete condition that means the task is finished.]
